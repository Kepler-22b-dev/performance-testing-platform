# 性能测试平台 - 系统架构手册

> 版本：3.2 | 更新时间：2026-07-17

---

## 一、平台概述

性能测试平台是一个基于 Apache JMeter 的分布式性能测试平台，提供 Web 界面管理压测任务、分析结果、监控资源，并支持移动端性能监控。

### 核心能力

| 能力 | 说明 |
|------|------|
| 🎯 压测执行 | 分布式 JMeter 压测，支持多 Slave 节点 |
| 📊 结果分析 | TPS/RT/错误率曲线、分布图、百分位分析 |
| 📱 移动端监控 | iOS (pymobiledevice3) + Android (SOLOX) 性能采集 |
| ⏱️ 定时调度 | 间隔执行、单次定时 |
| 🔔 告警通知 | 阈值检测 + Webhook 通知 |
| 📈 趋势分析 | 历史性能趋势、任务对比 |

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户浏览器                                  │
│                        http://管理节点IP:8000                            │
└───────────────────────────────────┬─────────────────────────────────────┘
                                    │ HTTP / WebSocket
┌───────────────────────────────────▼─────────────────────────────────────┐
│                           Manager 服务                                   │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐   │
│  │   任务管理    │ │   脚本编辑   │ │   结果分析   │ │   节点管理   │   │
│  │   模板引擎    │ │   定时调度   │ │   通知系统   │ │   资源监控   │   │
│  │   告警规则    │ │   配置管理   │ │   JTL对比    │ │   日志系统   │   │
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘   │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    移动端性能监控模块                              │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │   │
│  │  │ iOS Monitor │  │Android Mon. │  │  设备检测    │             │   │
│  │  │(pymobiledev)│  │   (SOLOX)   │  │  应用列表    │             │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘             │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└───────┬─────────────────────────┬─────────────────────┬─────────────────┘
        │ Redis Streams           │ Redis Streams       │ Redis Streams
┌───────▼───────────┐  ┌─────────▼─────────┐  ┌────────▼────────────────┐
│       Redis       │  │     Agent 1       │  │     Agent 2 / N         │
│       :6379       │  │     (施压机1)      │  │     (施压机2/N)          │
│  消息队列+持久化   │  │      :9999        │  │      :9999              │
└───────────────────┘  └────────┬──────────┘  └────────┬────────────────┘
                                │                       │
                         ┌──────▼──────┐       ┌───────▼──────┐
                         │ JMeter Slave│       │ JMeter Slave │
                         │    :1100    │       │    :1200     │
                         └─────────────┘       └──────────────┘
```

### 2.2 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| **前端** | HTML/CSS/JS | - | 原生单页应用，无框架依赖 |
| **图表** | ECharts | 5.4.3 | 实时曲线、对比图表、趋势分析 |
| **后端** | Python + FastAPI | 3.10+ | 高性能异步 API 框架 |
| **消息队列** | Redis | 6.2+ | Streams 可靠通信 + 实时状态 |
| **压测引擎** | Apache JMeter | 5.6.3 | 行业标准压测工具 |
| **通信协议** | Redis Streams | - | Manager ↔ Agent 可靠通信、Pending 回收与幂等处理 |
| **iOS监控** | pymobiledevice3 | 9.x | iOS 设备性能数据采集 |
| **Android监控** | SOLOX | 2.9.x | Android/iOS 性能数据采集 |

### 2.3 数据流

```
压测流程:
用户操作 → Web UI → FastAPI API → Agent 定向 Redis Stream → Agent → JMeter
                                                          ↓
用户看到 ← WebSocket ← FastAPI ← Redis Streams ← Agent ← 执行结果

移动端监控:
iPhone/Android → USB → pymobiledevice3/SOLOX → FastAPI API → Web UI (ECharts)
```

---

## 三、模块详解

### 3.1 Manager 服务

Manager 是平台的核心服务，负责任务调度、结果收集、API 提供。

```
manager/
├── main.py                 # FastAPI 应用入口
├── api/                    # API 路由层（16个模块）
│   ├── tasks.py           # 任务管理
│   ├── scripts.py         # 脚本管理
│   ├── results.py         # 结果分析
│   ├── nodes.py           # 节点管理
│   ├── data.py            # 数据管理（变量/CSV）
│   ├── monitor.py         # 系统监控
│   ├── slave.py           # JMeter Slave 控制
│   ├── registry.py        # 远程节点注册
│   ├── templates.py       # 测试计划模板
│   ├── notifications.py   # Webhook 通知
│   ├── scheduler_api.py   # 定时调度
│   ├── alerts.py          # 告警规则
│   ├── environments.py    # 多环境管理
│   ├── jtl_compare.py     # JTL 文件对比
│   ├── tool_logs.py       # 工具日志
│   └── mobile.py          # 移动端监控
├── core/                  # 核心业务逻辑
│   ├── scheduler.py       # 任务调度器
│   ├── node_manager.py    # Agent 节点管理
│   ├── node_registry.py   # 远程节点注册表
│   ├── monitor.py         # 系统资源监控
│   ├── slave_manager.py   # JMeter Slave 管理
│   ├── variables.py       # 变量和 CSV 管理
│   ├── sample_cache.py    # 采样数据缓存
│   ├── ws.py              # WebSocket 管理
│   ├── mobile_monitor.py  # 移动端监控核心
│   ├── db.py              # 数据库操作
│   └── db_sync.py         # 数据库同步
├── static/
│   └── index.html         # 前端单页应用
└── models/
    └── db_models.py       # 数据库模型
```

### 3.2 Agent 服务

Agent 运行在施压节点上，接收 Manager 的任务指令并执行 JMeter。

```
agent/
├── main.py                # Agent 主程序
├── jmeter_runner.py       # JMeter 执行器
└── requirements.txt       # Agent 依赖
```

### 3.3 移动端监控模块

支持 iOS 和 Android 双引擎的移动端性能监控。

#### iOS 监控原理

```
iPhone (USB) → pymobiledevice3 → sysmon process single --userspace
                                       ↓
                                  JSON 数据
                                       ↓
                              CPU: cpuUsage (%)
                              Memory: physFootprint (bytes → MB)
                              PID: pid
```

#### Android 监控原理

```
Android (USB) → SOLOX 服务 (:50001) → HTTP API
                                       ↓
                                  JSON 数据
                                       ↓
                              CPU: cpuUsage (%)
                              Memory: total (MB)
                              FPS: fps (Hz)
                              Network: upload/download (KB)
                              Battery: level (%)
```

#### iOS 监控限制

- 需要 iOS 17+ 支持 `--userspace` 模式（无需 sudo）
- `sysmon process single` 命令首次调用约需 20 秒
- 不支持 FPS、网络、电池等指标
- 进程名匹配需要通过 `CFBundleExecutable` 字段

#### 异步架构

为避免阻塞 FastAPI 事件循环，所有 pymobiledevice3 调用使用 `run_in_executor` 异步执行：

```python
@staticmethod
async def _run_cmd_async(cmd: List[str], timeout: int = 15) -> Optional[str]:
    import asyncio
    return await asyncio.get_event_loop().run_in_executor(
        None, IOSMonitor._run_cmd, cmd, timeout
    )
```

---

## 四、API 接口

### 4.1 核心 API

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/health | GET | 健康检查 |
| /api/tasks/ | GET | 任务列表 |
| /api/tasks/quick-run | POST | 快速执行压测 |
| /api/scripts/ | GET | 脚本列表 |
| /api/results/tasks | GET | 结果列表 |
| /api/results/trend | GET | 性能趋势 |
| /api/results/compare | GET | 任务对比 |

### 4.2 移动端监控 API

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/mobile/status | GET | 检查 SOLOX/pymobiledevice3 状态 |
| /api/mobile/detect | GET | 自动检测设备平台和信息 |
| /api/mobile/devices | POST | 获取已连接设备列表 |
| /api/mobile/apps/{platform} | GET | 获取已安装应用列表 |
| /api/mobile/collect/all | POST | 采集所有性能指标 |
| /api/mobile/config | GET | 获取配置信息 |

### 4.3 移动端监控数据格式

```json
{
  "cpu": {"cpu": 16.14},
  "memory": {"total": 607.05},
  "pid": "6139",
  "name": "WeChat"
}
```

---

## 五、目录结构

```
PerformanceTestingPlatform/
├── common/                     # 公共模块
│   ├── config.py              # 全局配置（Redis、JMeter、SOLOX等）
│   ├── protocol.py            # 通信协议
│   ├── logger.py              # 统一日志模块
│   ├── database.py            # 数据库工具
│   ├── async_worker.py        # 异步任务执行
│   └── utils.py               # 通用工具函数
│
├── manager/                    # Manager 服务
│   ├── main.py                # FastAPI 应用入口
│   ├── api/                   # API 路由层
│   ├── core/                  # 核心业务逻辑
│   ├── static/index.html      # 前端单页应用
│   └── requirements.txt       # Manager 依赖
│
├── agent/                      # Agent 服务
│   ├── main.py                # Agent 主程序
│   ├── jmeter_runner.py       # JMeter 执行器
│   └── requirements.txt       # Agent 依赖
│
├── scripts/                    # JMeter 脚本目录
├── reports/                    # 压测报告目录
├── logs/                       # 日志目录
├── docs/                       # 项目文档
│   ├── change-log.md          # 变更日志
│   ├── ecommerce-order-flow-jmeter.md  # 电商订单流压测
│   ├── mock-ecommerce-pressure-test.md # 模拟电商压测
│   └── samples-aggregation-plan.md     # 采样聚合方案
├── mock-data/                  # 模拟数据目录
├── mock-server.py              # 模拟服务器
├── tests/                      # 测试用例
├── cli.py                      # CLI 命令行工具
├── package.json                # Node.js 配置（前端工具）
├── deploy.sh                   # 一键部署脚本
├── start-slave.sh              # Slave 节点启动脚本
├── 项目说明.md                   # 项目说明
├── 架构手册.md             # 本文档
└── 部署手册.md                   # 部署手册
```

---

## 六、配置说明

### 6.1 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| REDIS_HOST | 127.0.0.1 | Redis 主机 |
| REDIS_PORT | 6379 | Redis 端口 |
| REDIS_DB | 0 | Redis 数据库 |
| REDIS_USERNAME | 空 | Redis ACL 用户名 |
| REDIS_PASSWORD | 空 | Redis 密码 |
| REDIS_SSL | false | 是否启用 Redis TLS |
| DATABASE_URL | postgresql+asyncpg://localhost:5432/perftest | PostgreSQL 异步连接地址 |
| COMMAND_TTL_SECONDS | 300 | Agent 命令有效期 |
| COMMAND_CLAIM_IDLE_MS | 30000 | Pending 命令回收等待时间 |
| COMMAND_DEDUP_TTL_SECONDS | 604800 | 命令幂等记录保留时间 |
| CORS_ALLOWED_ORIGINS | localhost/127.0.0.1:8000 | 允许的跨域前端来源 |
| JMETER_HOME | ./apache-jmeter-5.6.3 | JMeter 安装路径 |
| SOLOX_HOST | 127.0.0.1 | SOLOX 服务主机 |
| SOLOX_PORT | 50001 | SOLOX 服务端口 |
| MAX_CONCURRENT_TASKS | 3 | 最大并发任务数 |
| TASK_TIMEOUT | 3600 | 任务超时时间（秒） |

### 6.2 Redis 频道

| 频道 | 说明 |
|------|------|
| jmeter:result | 压测结果频道 |
| jmeter:heartbeat | Agent 心跳频道 |
| jmeter:command:{agent_id} | Agent 定向命令 Stream |
| jmeter:progress | 进度更新频道 |

---

## 七、关键流程

### 7.1 任务执行流程

```
1. 用户创建任务 → Web UI
2. 提交到 API → /api/tasks/quick-run
3. 任务调度器分配节点 → scheduler.py
4. 通过 Redis 下发命令 → jmeter:command:{agent_id} Stream
5. Agent 接收命令 → agent/main.py
6. 启动 JMeter 执行 → jmeter_runner.py
7. 实时推送进度 → jmeter:progress 频道
8. 执行完成，收集结果 → jmeter:result 频道
9. 生成报告 → reports/
10. 通知用户 → WebSocket + Webhook
```

### 7.2 移动端监控流程

```
1. 页面加载 → autoDetectDevice()
2. 自动检测设备平台 → /api/mobile/detect
3. 加载设备列表和应用列表 → /api/mobile/apps/{platform}
4. 用户选择应用 → onAppSelect()
5. 点击开始监控 → startMobileMonitor()
6. 定时采集数据 → /api/mobile/collect/all (每5秒)
7. 后端调用 pymobiledevice3/SOLOX
8. 返回 CPU/内存数据
9. 更新 ECharts 图表
```

---

## 八、性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| API 响应时间 | < 100ms | 不含移动端采集 |
| 移动端采集延迟 | < 30s | iOS sysmon 首次调用 |
| WebSocket 推送延迟 | < 1s | 实时进度更新 |
| 最大并发任务数 | 3-10 | 可配置 |
| 历史数据查询 | < 500ms | PostgreSQL |
| 图表渲染 | < 100ms | ECharts |

---

## 九、安全考虑

1. **Redis 安全**：生产环境配置密码、限制访问 IP
2. **网络安全**：使用防火墙限制端口访问
3. **数据安全**：定期备份脚本和报告
4. **访问控制**：仅允许通过内网、VPN、反向代理白名单或防火墙访问，不建设应用内登录
5. **日志管理**：定期清理过期日志和报告

---

## 十、演进方向

### 平台定位

目标不只是执行 JMeter 脚本，而是逐步升级为**内部性能风险评估与容量决策平台**。

### 演进原则

1. 保留“模块化单体 Manager + 独立 Agent”的总体结构，近期不拆分大量微服务。
2. 先解决任务可靠性、分布式制品和数据一致性，再做 Manager 横向扩容和 Agent 自动扩缩。
3. PostgreSQL 保存业务事实，Redis 保存通信和短期实时状态，对象存储保存脚本、CSV、JTL、日志和报告。
4. 新增能力必须支持幂等、失败重试、可观测和自动化测试。

### 总体路线图

| 阶段 | 内容 | 优先级 |
|------|------|--------|
| P0：分布式基础补强 | 任务一致性、可靠命令、制品存储、数据库迁移、安全基线 | 最高 |
| P1：工程化与数据能力 | 依赖锁定、容器化、CI、结果聚合、可观测性、前端模块化 | 高 |
| P2：高可用与弹性资源池 | Scheduler 独立、多 Manager、Agent 负载调度、Docker 扩缩 | 中 |
| P3：平台能力扩展 | 性能基线、CI/CD 门禁、多引擎、TimescaleDB、AI 诊断、全链路压测 | 中/低 |

### P0：分布式基础补强

当前平台定位为内部可信网络工具，不建设登录认证系统；访问边界由内网、反向代理、VPN 和防火墙控制。

#### 任务与消息可靠性

- [x] 将 Agent 心跳与命令消费拆成独立循环，心跳严格按 5～10 秒发送。
- [x] 命令增加 `command_id`、`created_at`、`expires_at`、`attempt` 和协议版本。
- [x] Agent 在 Redis 中持久化已处理命令，避免重启或重复投递导致任务重复执行。
- [x] 将公共命令 Stream 改为每 Agent 独立 Stream。
- [x] 增加 Pending 消息检查和 `XAUTOCLAIM`，处理消费者异常退出后的未确认消息。
- [x] Agent 只在命令校验并持久化接收状态后确认消息。
- [x] PostgreSQL 使用条件更新原子完成 `pending → running`，防止并发重复启动。
- [x] 为 `task_results(task_id, agent_id)` 增加唯一约束并改为 upsert。
- [x] 定义并校验任务状态机，明确停止、超时、部分 Agent 失败和终态不可回退。

#### 分布式制品存储

- [x] 引入可切换的本地文件系统与 MinIO/S3 兼容制品存储基础层。
- [ ] 脚本、CSV、插件和其他输入文件统一生成制品 ID、版本和校验和（JMX 和 CSV 已完成，插件待接入）。
- [x] Agent 执行 JMX 前下载制品并校验 SHA-256，不再依赖 Manager 本地绝对路径。
- [x] CSV 参数文件改为 Agent 按制品引用下载，并校验 SHA-256。
- [x] 增加 CSV 多 Agent 均衡分片模式，同时保留每个 Agent 下载完整 CSV 的复制模式。
- [ ] 插件等其他任务依赖也改为 Agent 按制品引用下载。
- [ ] Agent 完成后上传 JTL、错误响应、JMeter 日志和导出报告。
- [ ] Manager 报告接口改为读取对象存储或聚合数据库，不再扫描本机 `reports/` 作为唯一数据源。
- [ ] 制定制品保留、归档、重建和清理策略。

#### 数据库与配置治理

- [x] 统一 `DATABASE_URL` 默认值和配置入口，移除重复定义。
- [x] 完成 Alembic 初始迁移，启动时不再依赖 `create_all()` 代替版本迁移。
- [x] 增加任务结果外键、唯一约束、必要索引和数据库升级/回滚验证。
- [ ] 时间字段逐步从浮点时间戳迁移为带时区时间类型。

#### 安全基线

- [ ] 通过内网、VPN、反向代理访问策略和防火墙限制平台入口，不在应用内建设登录认证。
- [ ] 收紧 CORS、WebSocket Origin 和上传接口限制。
- [x] Redis 支持用户名、密码、TLS 和连接超时配置。
- [ ] 数据库、Webhook Token、环境认证信息改用密钥管理或加密存储。
- [ ] 对 Webhook、环境探测等外部 URL 增加 SSRF 防护和访问审计。

### P1：工程化与数据能力

#### 构建、测试与部署

- [ ] 使用锁文件固定 Python 直接和间接依赖，建立定期升级流程。
- [ ] 补充 `pyproject.toml`，统一测试、格式化、静态检查和类型检查配置。
- [ ] 提供 Manager、Agent 的 Dockerfile 和本地 Docker Compose 环境。
- [ ] CI 自动执行 Python 单元测试、数据库迁移测试和 Playwright 主流程测试。
- [ ] 增加 PostgreSQL、Redis、对象存储和多 Agent 的集成测试。

#### 结果数据与查询

- [ ] 按 `docs/samples-aggregation-plan.md` 实现秒级 `sample_aggregates` 聚合表。
- [ ] 任务完成后异步生成任务级和接口级统计，报告优先读取聚合结果。
- [ ] 使用 t-digest 或 HDR Histogram 保存可合并的百分位数据。
- [ ] 保留原始 JTL 的审计和重建能力，历史任务可只保留聚合与导出报告。
- [ ] 数据量达到 PostgreSQL 单表瓶颈后，再评估 TimescaleDB 或分区表。

#### 可观测性与维护性

- [ ] 增加 Prometheus 指标：任务队列、任务耗时、失败率、Agent 在线数、消息积压和 API 延迟。
- [ ] 增加 request ID、task ID、command ID 的关联日志。
- [ ] 接入 OpenTelemetry，跟踪 Manager、Redis、Agent 和 JMeter 启动链路。
- [ ] 减少静默吞掉异常的 `except Exception: pass`，统一错误分类和告警。
- [ ] 前端继续使用原生 HTML/CSS/JavaScript；后续仅做文件拆分和模块化，暂不迁移 Vue/React。

### P2：高可用与弹性资源池

- [ ] 将定时调度和任务分发从 Web 进程拆成独立 Scheduler Worker。
- [ ] 使用 PostgreSQL advisory lock 或可靠租约确保同一调度只触发一次。
- [ ] 实时进度从 Manager 内存迁移到 Redis/时序存储。
- [ ] WebSocket 增加跨 Manager 实例事件广播。
- [ ] NodeManager 从 Redis Hash/TTL 重建完整节点视图，避免多 Manager 消费到不同心跳片段。
- [ ] Manager API 达到无状态后再部署多个实例和负载均衡。
- [ ] 完成 Agent 资源评分、任务排队、自动选择和容量保护。
- [ ] 在资源池稳定后实施 Docker 动态扩缩，最后再评估 Kubernetes HPA。

### P3：平台能力扩展

- [ ] 性能基线、历史趋势和自动回归检测。
- [ ] CI/CD 性能门禁和标准化退出码/报告接口。
- [ ] 抽象 `LoadEngine` 接口，逐步支持 k6、Locust 和自定义引擎。
- [ ] 支持多租户、项目空间、配额、审计日志和成本统计。
- [ ] AI 辅助异常归因、容量建议和报告摘要。
- [ ] 全链路压测、流量染色、影子环境和生产安全保护。

### 实施顺序与依赖

```text
数据库迁移与任务状态机
          ↓
可靠命令投递 ─────→ Agent 幂等执行
          ↓                 ↓
对象存储与制品分发 → 结果上传与聚合
          ↓                 ↓
可观测性与安全基线 → 单 Manager 稳定运行
                              ↓
                Scheduler 独立与多 Manager
                              ↓
                    Agent 自动扩缩与多引擎
```

### 插件化测试引擎

```
┌─────────────────────────────────────┐
│         统一任务调度层               │
├──────┬──────┬──────┬───────────────┤
│ JMeter│  k6  │ Locust│ 自定义引擎   │
│ 引擎  │ 引擎 │ 引擎  │  (SDK)       │
└──────┴──────┴──────┴───────────────┘
```

### 动态压力机扩容

#### 目标

实现 Agent 节点的自动发现、负载感知和动态扩缩容，无需人工干预即可按需扩展施压能力。

#### 三阶段演进路线

**阶段一：Agent 自注册 + 资源池（近期）**

在 P0/P1 基础稳定后，基于现有 Redis Streams 和节点 Hash 完善资源池：

```
新 Agent 启动
  → 连接 Redis → 自动注册到 jmeter:agents 集合
  → 上报资源指标（CPU/内存/磁盘/端口）到 Redis Hash
  → Manager 心跳检测发现新节点，自动纳入调度池
  → 任务调度时按负载评分选择最优 Agent
  → 空闲超时 → Agent 自动退出
```

涉及模块改动：
- `agent/main.py`：启动时上报资源信息，心跳携带实时指标
- `manager/core/scheduler.py`：`get_available_agent()` 改为按负载评分选择
- `manager/core/node_manager.py`：增加资源感知，支持动态扩缩策略
- `manager/core/auto_scaler.py`（新增）：监控负载，触发扩缩决策
- `common/config.py`：新增 `AGENT_IDLE_TIMEOUT`、`AGENT_MAX_SCALE`、`AGENT_COOLDOWN` 等配置

调度策略：
- 扩容触发：任务排队 > 0 且 当前 Agent 数 < `AGENT_MAX_SCALE`
- 缩容触发：所有 Agent 空闲超过 `AGENT_IDLE_TIMEOUT`
- 冷却时间：扩缩操作间隔 ≥ `AGENT_COOLDOWN`
- 负载评分：`CPU空闲度×0.4 + 内存空闲度×0.3 + 磁盘空间×0.2 + 空闲状态×0.1`

**阶段二：Docker 动态扩缩（中期）**

Agent 容器化，Manager 通过 Docker API 动态创建/销毁容器：

```
Manager 检测到任务排队
  → 调用 Docker API 创建 Agent 容器
  → 新容器自动连接 Redis 注册
  → 任务分配完成
  → 空闲超时 → Manager 调用 Docker API 停止容器
```

涉及模块改动：
- 新增 `Dockerfile.agent`：Agent 镜像打包
- 新增 `manager/core/docker_scaler.py`：Docker API 封装
- `manager/core/scheduler.py`：任务排队时触发扩容

扩缩决策参数：
- 最大实例数：`AGENT_MAX_SCALE`（默认 10）
- 最小实例数：`AGENT_MIN_SCALE`（默认 1）
- 冷却时间：`AGENT_COOLDOWN`（默认 120s）
- 镜像名称：`AGENT_DOCKER_IMAGE`（可配置）

**阶段三：Kubernetes HPA（远期）**

企业级方案，适合大规模团队和云原生环境：

```
Agent K8s Deployment (min=1, max=10)
  → 自定义 Metrics Adapter 暴露 jmeter:queue_length
  → HPA 根据队列长度自动扩缩 Pod
  → Pod 启动时自动注册到 Redis
  → 任务完成后缩容到 min
```

需要的基础设施：
- K8s 集群 + Metrics Server
- 自定义 Metrics Adapter（暴露 Redis 队列长度）
- Agent Docker 镜像
- ConfigMap 管理配置

#### 方案选型建议

| 场景 | 推荐方案 |
|------|----------|
| 当前阶段快速验证 | 阶段一（自注册 + 资源池） |
| 已用 Docker 部署 | 阶段二（Docker 动态扩缩） |
| 企业级大规模 | 阶段三（Kubernetes HPA） |

---

## 十一、维护记录

- 2026-07-04：初始架构设计，搭建基础框架
- 2026-07-05：完善任务管理、脚本管理、结果分析
- 2026-07-06：新增模板管理、定时调度、告警规则
- 2026-07-07：新增移动端性能监控（iOS + Android 双引擎）
- 2026-07-07：文档整合优化
- 2026-07-16：修复创建任务流程 16 项问题（任务ID原子生成、Redis发布重试回滚、JTL缓存LRU、Agent持久化身份等）
- 2026-07-16：规划动态压力机扩容三阶段路线（自注册+资源池 → Docker扩缩 → K8s HPA）
- 2026-07-17：基于架构评审补充分布式可靠性、对象存储、数据治理、安全、高可用和工程化路线图
